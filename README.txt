JWT is JSON Web token 
This is used for authentication of user
The basic idea is :
USer sends the username /pwd to the server
The server validates the user - and generates a JWT is user is present
Now the user will use this JWT for all his other requests as long as the JWT is alive